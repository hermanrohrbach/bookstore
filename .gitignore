#Ignore all node modules, and install them again locally
node_modules/

#Ignore everything in the build folder, and compile again locally
build/

#package-lock.json is automatically updated
package-lock.json
